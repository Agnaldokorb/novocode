{
  "buildCommand": "npx prisma generate && npm run build",
  "installCommand": "npm install",
  "env": {
    "SKIP_ENV_VALIDATION": "1"
  },
  "functions": {
    "app/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "crons": []
}
